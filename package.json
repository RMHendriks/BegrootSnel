{
  "name": "begrootsnel-root",
  "version": "1.0.0",
  "description": "Root project for BegrootSnel with Angular frontend and Quarkus backend",
  "scripts": {
    "start": "concurrently -n \"frontend,backend\" -c \"blue,green\" \"npm run frontend\" \"npm run backend\"",
    "frontend": "cd BegrootSnelFrontEnd && npm start",
    "backend": "cd BegrootSnelBackEnd && ./mvnw quarkus:dev",
    "install:all": "npm install && cd BegrootSnelFrontEnd && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
